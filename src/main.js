// SAX namespace...It's a Metroid thing
window.SAX = window.SAX || {};

// Initialise all modules
window.SAX.init = function init () {
    var games = {};

    var utils = core.Utils(window, document);
    var dom = core.Dom(window, document);


    games.grid = function () {
        // Init grid game
    };

    games.memory = function () {
        // Init memory game
    };
};

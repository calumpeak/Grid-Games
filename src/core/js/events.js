var core = core || {};

core.Events = function Events (window, document, utils, dom) {
    //Bind
    //Unbind
    //UnbindAll
    //Observable
        //fire
        //on
        //once

    return {
        // Stuff
    };
};

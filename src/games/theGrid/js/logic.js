var theGrid = theGrid || {};

/**
 * Logic for the grid games
 *
 * @module theGrid
 * @submodule Logic
 */
theGrid.logic = function logic (grid, timer, score, page, utils, dom, events) {
    // Placeholder
    // Might rename - need to settle on structure for games
    return {

    };
};
